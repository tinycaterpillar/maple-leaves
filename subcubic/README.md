# Perfect Matching Checker for Degree Sequences

This Python script tests whether **all connected, bridgeless graphs** generated from a given degree sequence have a **perfect matching**.

It uses multiprocessing to efficiently experiment with many graphs, and if a graph is found **without a perfect matching**, the script immediately stops and displays that graph for inspection.
